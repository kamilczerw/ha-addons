# Changelog

## 16.0.2

- Initial release of the Forgejo add-on, wrapping Forgejo v16.0.2.
- Ingress-enabled with an automatic Root URL fix so the sidebar/Ingress path works out of the box.
- Direct HTTP (3000) and SSH (22, mapped to host 2222) ports exposed for `git` clone/push.
